<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.listenenglish">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.PlayActivity"></activity>
        <activity android:name=".activity.DanhSachBaiHocActivity" />
        <activity
            android:name=".activity.DanhSachKhoaHocBaiHocActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".activity.DanhSachKhoaHocActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".activity.DanhSachFileActivity"
            android:launchMode="singleInstance" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>